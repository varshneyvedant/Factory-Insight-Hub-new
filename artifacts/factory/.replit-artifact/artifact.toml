kind = "web"
previewPath = "/"
title = "Factory Manager"
version = "1.0.0"
id = "artifacts/factory"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20378

[services.development]
run = "pnpm --filter @workspace/factory run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/factory", "run", "build" ]
publicDir = "artifacts/factory/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20378"
BASE_PATH = "/"
